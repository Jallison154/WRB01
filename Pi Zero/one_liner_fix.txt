# One-liner fix for externally-managed-environment error
# Copy and paste this command into your terminal:

pip3 install --user --break-system-packages pygame pyserial numpy RPi.GPIO psutil

# If that doesn't work, try this alternative:

python3 -m pip install --user --break-system-packages pygame pyserial numpy RPi.GPIO psutil

# If you still get errors, try installing one package at a time:

pip3 install --user --break-system-packages pygame
pip3 install --user --break-system-packages pyserial  
pip3 install --user --break-system-packages numpy
pip3 install --user --break-system-packages RPi.GPIO
pip3 install --user --break-system-packages psutil

# Test if it worked:
python3 -c "import pygame, serial, numpy, psutil; print('All packages imported successfully!')"
